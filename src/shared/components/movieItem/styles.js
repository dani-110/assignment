import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
    main: {
        alignItems: 'center',
        marginVertical: 10,
        borderWidth: 1,
        borderRadius: 10,
        borderColor: 'grey',
        flexDirection: 'row'

    },
    imgView: {
        justifyContent: 'center',
        alignItems: 'center'
    }
})